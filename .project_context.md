# BGMS 프로젝트 컨텍스트 및 작업 가이드
*최종 업데이트: 2026-06-10 (Admin Agent 운영 자동화 안정화 및 무기 마스터리 전용 갱신 반영)*

이 문서는 BGMS 프로젝트에서 AI와 개발자가 빠르게 전체 흐름을 이해하고, 변경 지점을 정확히 찾기 위한 기준 문서입니다. 실제 코드를 우선 기준으로 삼고, 이 문서는 작업 방향과 주요 연결 관계를 보조합니다.

## 1. 프로젝트 정체성

BGMS는 PUBG 유저를 위한 전술 지도, 전적 검색, 텔레메트리 리플레이, 전술 분석, AI 코칭, 관리자 운영 보조를 제공하는 Next.js 애플리케이션입니다.

핵심 사용자 흐름은 다음과 같습니다.

```text
루트 접속
-> /maps/erangel 기본 리다이렉트
-> 지도/마커/핫드랍/제보/시뮬레이터 사용
-> 전적 검색 시 PUBG API 조회
-> 매치별 AnalysisEngine 분석
-> Cloudflare R2 하이브리드 캐시 및 Supabase DB 저장
-> 통계 UI, 지도 리플레이, AI 요약/코칭에서 재사용
```

관리자 운영 흐름은 다음과 같습니다.

```text
/admin/bot 접속
-> 운영판/AI 비서/승인 패널 확인
-> 조회성 진단은 즉시 실행
-> 삭제/발행/대량 변경은 승인 요청만 생성
-> agent_runs, agent_steps, agent_approvals, agent_memories에 기록
-> GitHub Actions가 일일 점검 snapshot을 남김
```

## 2. 현재 기술 스택

- 프레임워크: Next.js 16.1.1, App Router
- UI: React 19.2.3, React DOM 19.2.3
- 지도/3D: Leaflet, React Leaflet, leaflet.heat, Three.js
- 스타일: Tailwind CSS v4, 전역 CSS 변수 기반 테마
- 백엔드/인프라: Supabase Database, Auth, Realtime, Cloudflare R2 (AWS S3 SDK)
- AI: AI SDK, Google Generative AI
- 모바일: Capacitor v8
- 테스트: Jest, Vitest, ESLint

AI 모델 fallback 풀은 `app/api/pubg/ai-analyze/route.ts`와 `app/api/pubg/ai-summary/route.ts`의 실제 구현을 우선 확인합니다. 모델명은 비용, 응답 형식, 스트리밍 안정성, 런타임 지원 여부에 직접 영향을 주므로 코드와 운영 환경을 확인하지 않고 임의로 변경하지 않습니다.

## 3. 앱 라우팅과 화면 흐름

### 루트와 지도

- `app/page.tsx`: 레거시 `?tab=` 쿼리를 새 경로로 리다이렉트하고, 기본 접속은 `/maps/erangel`로 보냅니다.
- `app/maps/[mapId]/page.tsx`: 맵 ID를 받아 `HomeClient`에 전달합니다.
- `app/HomeClient.tsx`: 지도 앱의 클라이언트 진입점입니다.
- `components/map/MapShell.tsx`: 지도 화면의 중심 컨테이너입니다. 사이드바, 도구 메뉴, 시뮬레이터, 텔레메트리 재생 상태를 조합합니다.
- `components/map/MapView.tsx`: Leaflet 지도, 타일, 마커, 그리드, 핫드랍, 리포트 위치, 텔레메트리 레이어를 렌더링합니다.
- `lib/map_config.ts`: 맵 좌표, 타일, 아이콘, 좌표 변환, 고도 보정 설정을 관리합니다.

### 전적/분석

- `app/stats/page.tsx`: 전적 검색 진입점입니다.
- `app/stats/[platform]/[nickname]/page.tsx`: 플랫폼과 닉네임을 URL로 받은 전적 분석 페이지입니다.
- `app/stats/[platform]/[nickname]/weapons/page.tsx`: 무기 마스터리 전용 페이지입니다.
- `app/stats/[platform]/[nickname]/weapons/WeaponsClient.tsx`: 무기 숙련도 갱신 버튼, 전체 순위, 카테고리별 순위 UI를 담당합니다.
- `components/StatSearch.tsx`: 전적 검색과 매치 목록, 분석 UI의 주요 클라이언트 컴포넌트입니다.
- `components/stat/*`: 매치 카드, AI 요약, 레이더/스파이더 차트, 타임라인 등 분석 결과 표시 컴포넌트입니다.

### 커뮤니티/관리/도구

- `app/board/*`: 커뮤니티 게시판 목록, 상세, 작성 화면입니다.
- `app/admin/*`: 관리자용 리뷰, 맵 설정, 게임 데이터 관리, AI 비서 화면입니다.
- `app/admin/bot/page.tsx`: Admin Agent 운영 화면입니다. AI 비서, 승인 패널, 운영판, 수동 점검, 기억/실행 기록 UI가 여기에 모입니다.
- `app/api/admin/bot/run/route.ts`: 관리자 채팅형 도구 호출 진입점입니다.
- `app/api/admin/agent/*`: 운영 자동화 API입니다. monitor, approvals, rollout, self-test, runs, memories, briefing, handoff, incidents 등을 포함합니다.

### 텔레메트리 리플레이

`app/api/pubg/telemetry/route.ts`와 `hooks/useTelemetry.ts`가 리플레이 데이터 흐름의 중심입니다.

1. 지도에서 `?playback={matchId}&nickname={nickname}` 형태로 리플레이를 요청합니다.
2. `useTelemetry`가 `/api/pubg/telemetry`를 호출합니다.
3. R2 스토리지에 저장된 `{matchId}_{nickname}_v{TELEMETRY_VERSION}_map.json` 캐시를 우선 사용합니다.
4. 캐시가 없으면 PUBG 텔레메트리를 직접 받아 `AnalysisEngine`을 실행하고 `mapData`를 생성하여 R2에 업로드합니다.
5. `components/map/telemetry/*` 렌더러가 지도 위에 재생 데이터를 표시합니다.
6. 3D 리플레이는 대형 맵의 하이트맵, 고도 보정, 지상 스냅, 카메라/레이어 토글, 고해상도 타일 텍스처를 사용합니다.

3D 리플레이 관련 변경은 `app/replay/3d/page.tsx`, `lib/map_config.ts`, `hooks/useTelemetry.ts`, `components/map/telemetry/*`를 함께 확인합니다. 지형, 충돌, 고도, 카메라, 리플레이 ingest, fallback, 가시성 QA가 서로 연결되어 있습니다.

### AI 코칭/요약

- `app/api/pubg/ai-analyze/route.ts`: 단일 매치 기반 코칭 리포트입니다.
- `app/api/pubg/ai-summary/route.ts`: 여러 매치 집계 기반 최근 플레이 요약입니다.

두 API 모두 분석 결과를 사람이 읽기 좋은 프롬프트 데이터로 변환한 뒤 Gemini 계열 모델에 전달합니다. 응답은 JSON 또는 NDJSON 스트리밍 형태로 소비될 수 있으므로, 프롬프트 구조와 클라이언트 파싱 로직을 함께 검토해야 합니다.

## 4. PUBG API 호출 정책

PUBG API는 운영상 `1분 10회` 제한을 기준으로 보호합니다. 검색 체감 속도보다 호출 안정성이 더 중요합니다.

- 기본 전적 검색은 플레이어/시즌/매치 중심입니다.
- 기본 전적 검색에서는 무기 숙련도 API를 호출하지 않습니다.
- 새로고침성 재검색은 `_t` 또는 `refresh=true`로 짧은 메모리 캐시를 우회할 수 있습니다.
- 무기 숙련도는 `/api/pubg/player/weapon-mastery`에서만 갱신합니다.
- 무기 숙련도 갱신은 `pubg_player_cache.id`의 accountId를 사용합니다.
- accountId 캐시가 없으면 PUBG player lookup을 추가 호출하지 않고 `409`로 "전적 검색 먼저 필요"를 반환합니다.
- 최근 3시간 이내 `mastery_updated_at`이 있으면 PUBG API를 다시 호출하지 않고 DB 캐시를 반환합니다.
- `trackPubgRateLimit`와 `pubg_api_status`로 API 잔여량을 기록합니다.
- PUBG API 에러는 `reportPubgApiError`를 통해 운영 진단에 연결합니다.

무기 마스터리 파싱과 카테고리 분류는 `lib/pubg/weaponMastery.ts`를 우선 확인합니다. 카테고리는 전체, AR, SMG, DMR, SR, 샷건, LMG, 권총, 투척류, 근접/특수, 기타 기준으로 정리합니다.

## 5. 주요 사용자 기능 지도

처음 코드를 볼 때는 라우트 이름보다 "사용자가 무엇을 하러 들어오는가"를 기준으로 보면 이해가 빠릅니다.

- 전술 지도: `/maps/[mapId]`, `app/HomeClient.tsx`, `components/map/*`, `lib/map_config.ts`
- 전적 검색: `/stats`, `/stats/[platform]/[nickname]`, `components/StatSearch.tsx`, `app/api/pubg/player/route.ts`
- 매치 분석: `app/api/pubg/match/route.ts`, `lib/pubg-analysis/AnalysisEngine.ts`, `components/stat/*`
- 텔레메트리 리플레이: `app/api/pubg/telemetry/route.ts`, `hooks/useTelemetry.ts`, `components/map/telemetry/*`
- 3D 리플레이: `/replay/3d`, `app/replay/3d/page.tsx`, Three.js, 하이트맵/고도 보정 로직
- 무기 마스터리: `/stats/[platform]/[nickname]/weapons`, `app/api/pubg/player/weapon-mastery/route.ts`, `lib/pubg/weaponMastery.ts`
- 무기 도감: `/weapons`, `app/weapons/*`
- 가방 시뮬레이터: `/backpack`, `app/backpack/*`, `lib/backpackUtils.ts`
- 상자 시뮬레이터: `/crates`, `app/crates/*`, `app/actions/crates.ts`, `lib/crateUtils.ts`
- 랭킹: `/rankings`, `app/rankings/*`
- 배틀/스쿼드 분석: `/stats/battle`, `app/api/pubg/battle/route.ts`, `app/api/pubg/ai-squad/route.ts`, `app/api/pubg/squad-analyze/route.ts`
- 커뮤니티: `/board`, `app/board/*`, `app/api/posts/write/route.ts`
- 관리자 운영: `/admin/*`, `/admin/bot`, `app/api/admin/*`, `lib/admin-agent/*`

## 6. API 맵

API를 수정할 때는 기능별 책임을 먼저 구분합니다.

- `app/api/pubg/player/route.ts`: 플레이어 기본 전적, 시즌/랭크/일반전, 캐시 조회와 갱신
- `app/api/pubg/player/weapon-mastery/route.ts`: 무기 숙련도 전용 갱신, 3시간 쿨다운, DB 캐시 저장
- `app/api/pubg/match/route.ts`: 단일 매치 분석, R2/DB 캐시, `AnalysisEngine` 연결
- `app/api/pubg/telemetry/route.ts`: 지도 리플레이용 텔레메트리 가공 데이터 제공
- `app/api/pubg/ingest/route.ts`: 텔레메트리/매치 데이터 ingest 계열 진입점
- `app/api/pubg/ai-analyze/route.ts`: 단일 매치 AI 코칭
- `app/api/pubg/ai-summary/route.ts`: 여러 매치 기반 최근 플레이 요약
- `app/api/pubg/ai-squad/route.ts`, `app/api/pubg/squad-analyze/route.ts`: 스쿼드/팀 분석
- `app/api/pubg/battle/route.ts`: 배틀 비교/전적형 분석
- `app/api/pubg/hotdrop/route.ts`: 핫드랍 데이터 조회
- `app/api/pubg/suggest/route.ts`: 검색/추천 보조
- `app/api/admin/agent/monitor/route.ts`: 운영 snapshot, severity, alerts, recommendations
- `app/api/admin/agent/approvals/*`: 승인 대기 목록, 승인, 거절
- `app/api/admin/agent/rollout/route.ts`: 배포 준비 상태 점검
- `app/api/admin/agent/self-test/route.ts`: Agent 필수 구성 자체 점검
- `app/api/admin/agent/command-center/route.ts`: 운영판 데이터 조합
- `app/api/admin/agent/memories/*`: 운영 메모리 저장/조회/비활성화
- `app/api/admin/agent/runs/*`: Agent 실행 기록 조회
- `app/api/cron/*`: GitHub Actions 또는 Cron성 호출용 작업
- `app/api/report/*`: 지도 제보/투표/알림
- `app/api/images/*`: 무기/부착물/상자 이미지 프록시
- `app/api/og/*`, `opengraph-image.tsx`: 공유용 OG 이미지 생성

## 7. 데이터와 마이그레이션

Supabase 핵심 테이블과 도메인별 용도는 `mermaid.md`를 기준 문서로 봅니다. `.project_context.md`에는 전체 작업 흐름만 남기고, 테이블 상세 설명은 중복 관리하지 않습니다.

스키마 변경은 `supabase/migrations/*`를 기준으로 확인합니다. 운영 DB를 직접 만지기 전에는 마이그레이션 파일, `mermaid.md`, 실제 테이블 사용처를 함께 봅니다.

- `20260522141500_crate_simulator.sql`, `20260522142400_crate_bonus_shop.sql`: 상자 시뮬레이터/보너스 상점 데이터
- `20260526022000_auth_profiles_trigger.sql`: Auth 사용자와 `profiles` 동기화
- `20260526031000_admin_monitoring.sql`: 관리자 모니터링 기반 테이블
- `20260604000000_pubg_api_errors.sql`: PUBG API 에러 기록
- `20260610020000_admin_agent_foundation.sql`: Admin Agent 기초 테이블

특히 다음 흐름은 자주 수정되므로 `mermaid.md`와 코드 사용처를 같이 확인합니다.

- 전술 분석 캐시: `match_master_telemetry`, `processed_match_telemetry`, `match_stats_raw`, `global_benchmarks`
- 플레이어/무기 숙련도 캐시: `pubg_player_cache`, `weapon_mastery_data`, `mastery_updated_at`
- PUBG API 운영 상태: `pubg_api_errors`, `pubg_api_status`
- Admin Agent 기록: `agent_runs`, `agent_steps`, `agent_approvals`, `agent_memories`
- AI 비용/캐시: `ai_usage_logs`, `match_ai_coaching_cache`, `player_ai_summary_cache`, `squad_ai_coaching_cache`
- 도감/시뮬레이터 데이터: `weapons`, `attachments`, `ammo`, `consumables`, `throwables`, `vehicles`, `crate_templates`, `crate_items`

## 8. Admin Agent 운영 원칙

Admin Agent는 자동 실행 봇이 아니라, 무료 플랜에서도 안전하게 굴러가는 운영 감시/승인 보조 시스템입니다.

- 서버에서도 admin role을 검증합니다.
- Service Role Key는 서버 라우트 안에서만 사용합니다.
- 조회성 진단은 바로 실행할 수 있습니다.
- 게시글 발행, 캐시 삭제, 유저 조작, 대량 수정 같은 위험 작업은 승인 요청만 생성합니다.
- 승인 요청에는 영향도 미리보기를 붙입니다.
- 승인 실행 직전에도 영향도를 다시 계산하고 결과 로그에 포함합니다.
- 모든 실행은 `agent_runs`, `agent_steps`, `agent_approvals`, `agent_memories` 기준으로 추적합니다.
- Discord 알림은 위험 조건이 있을 때만 보냅니다.
- `DISCORD_WEBHOOK_URL`이 없으면 알림은 조용히 건너뜁니다.

주요 코드 위치는 다음과 같습니다.

- `lib/admin-agent/tools.ts`: Agent가 사용할 수 있는 도구 정의와 안전 등급
- `lib/admin-agent/approvals.ts`: 승인 요청 생성/조회
- `lib/admin-agent/approval-execution.ts`: 승인된 작업 실행
- `lib/admin-agent/impact.ts`: 승인 영향도 계산
- `lib/admin-agent/monitor-trend.ts`: 운영 snapshot 추세 계산
- `lib/admin-agent/thresholds.ts`: 비용, 에러, PUBG API 잔여량 등 임계치
- `lib/admin-agent/logging.ts`: 실행 로그 기록

## 9. 관리자 하루 운영 루틴

관리자는 코드를 몰라도 `/admin/bot` 기준으로 하루 운영 상태를 확인할 수 있어야 합니다.

### 하루 시작

1. `/admin/bot`에서 운영판을 먼저 봅니다.
2. 승인 대기 수, PUBG API 에러, API 잔여량, AI 비용, 배포 상태를 확인합니다.
3. `수동 점검`, `운영판 보기`, `최종 보고` 계열 버튼으로 Agent snapshot을 갱신합니다.
4. 승인 대기 작업이 있으면 impact summary를 보고 승인/거절/보류를 결정합니다.
5. 위험 작업은 채팅으로 바로 실행하지 않고 승인 큐를 거칩니다.

### 문제 발생 시

1. monitor alert의 severity와 message를 먼저 확인합니다.
2. PUBG API 문제면 `pubg_api_status`, `pubg_api_errors`, 최근 검색/매치 분석 API를 같이 봅니다.
3. AI 비용 문제면 `ai_usage_logs`와 AI 요약/코칭 호출부를 확인합니다.
4. 배포 문제면 GitHub Actions, Vercel deployment, `/api/admin/agent/rollout`을 같이 봅니다.
5. 캐시/텔레메트리 문제면 R2 상태, `processed_match_telemetry`, `TELEMETRY_VERSION`을 확인합니다.

### 하루 마감

1. Daily Checkout 또는 최종 보고를 실행합니다.
2. 남은 승인 대기, monitor alert, rollout readiness fail을 정리합니다.
3. 해결하지 못한 이슈는 handoff/incident/memory로 남깁니다.
4. 반복 이슈는 `agent_memories`에 저장해 다음 대응에 재사용합니다.

## 10. 자동화와 무료 플랜 운영

Vercel 무료 플랜을 고려해 별도 Vercel Cron을 늘리지 않습니다. 긴 작업은 기존 GitHub Actions에 남기고, Agent는 관찰, 기록, 알림, 승인 영향 분석을 담당합니다.

`.github/workflows/daily-tasks.yml`의 주요 역할은 다음과 같습니다.

- R2 스토리지 상태 점검
- 오래된 텔레메트리/AI 캐시 정리
- 스마트 스크래퍼 실행
- 블루존 데이터 추출
- 패치노트 동기화
- Agent Monitor Snapshot 기록
- Agent Rollout Readiness Snapshot 기록

`/api/admin/agent/monitor`는 `severity`, `alerts`, `recommendations`를 반환합니다. 정상 상태는 `agent_runs`에만 기록하고 Discord 알림은 보내지 않습니다. 위험 조건이 있을 때만 짧은 운영 알림을 보냅니다.

## 11. 주요 폴더 역할

### `app/`

Next.js App Router의 라우트와 API가 모여 있습니다.

- `layout.tsx`: 전역 레이아웃, 메타데이터, AuthProvider 래핑
- `globals.css`: Tailwind v4 및 전역 테마 변수
- `actions/map-settings.ts`: 맵 설정 관련 Server Action
- `api/pubg/*`: PUBG API, R2 텔레메트리 연동, AI 분석, 벤치마크, 무기 마스터리 갱신 API
- `api/admin/*`: 관리자 승인/거절/게임 데이터 관리, Admin Agent API
- `api/cron/*`: 핫드랍, 패치노트 등 예약 작업 진입점

### `components/`

UI와 클라이언트 상호작용 컴포넌트가 모여 있습니다.

- `components/map/*`: 전술 지도, 도구, 제보, 시뮬레이터, 텔레메트리 재생
- `components/map/telemetry/*`: 리플레이 전용 Canvas/레이어 렌더러
- `components/stat/*`: 전적 분석 카드, 차트, AI 요약, 매치 타임라인
- `components/board/*`: 게시판 UI
- `components/admin/*`: 관리자 편집 UI
- `components/common/*`: 글로벌 헤더, 모바일 메뉴, 하단 내비게이션, 푸터

### `hooks/`

클라이언트 비즈니스 로직을 캡슐화합니다.

- `useTelemetry.ts`: 리플레이 데이터 요청, 재생 시간, 플레이어 상태 보간
- `useMapData.ts`: 맵별 마커/제보 데이터 관리
- `useMapSettings.ts`: 맵 카테고리/아이콘 설정 동기화
- `useRealtimeToast.ts`: Supabase Realtime 기반 알림

### `lib/`

공유 설정, 분석 엔진, 도메인 유틸리티가 모여 있습니다.

- `lib/pubg-analysis/AnalysisEngine.ts`: 전술 분석 엔진 진입점
- `lib/pubg-analysis/handlers/*`: 전투, 위치, 자기장, 유틸리티, 리플레이 처리
- `lib/pubg-analysis/r2Service.ts`: Cloudflare R2 버킷 업로드/다운로드/배치 삭제 인터페이스
- `lib/pubg-analysis/constants.ts`: 분석/텔레메트리 버전, 맵/무기 상수, 임계값
- `lib/pubg-analysis/pubgApiTracker.ts`: PUBG API 호출 한도와 에러 추적
- `lib/pubg/weaponMastery.ts`: 무기 숙련도 파싱, 정렬, 카테고리 분류
- `lib/admin-agent/*`: 운영 진단, 승인, 알림, 영향도, 운영판 데이터 생성
- `lib/map_config.ts`: 맵 좌표, 타일, 아이콘, 좌표 변환 설정
- `lib/seo-config.ts`: 동적 SEO 설정
- `lib/supabase.ts`, `lib/supabase-middleware.ts`: Supabase 클라이언트/세션 보조

### `utils/supabase/`

Next.js 서버/클라이언트 환경에 맞춘 Supabase SSR 클라이언트 생성 유틸리티입니다.

## 12. 변경 작업 시 우선 확인할 것

### 분석 엔진 수정

`lib/pubg-analysis/*`를 수정할 때는 아래 호출부를 같이 확인합니다.

- `app/api/pubg/match/route.ts`
- `app/api/pubg/telemetry/route.ts`
- `app/api/pubg/ingest/route.ts`
- `app/api/pubg/ai-summary/route.ts`
- `app/api/pubg/ai-analyze/route.ts`
- `components/stat/*`
- `hooks/useTelemetry.ts`

분석 결과 타입, `mapData` 구조, 캐시 버전이 바뀌면 `RESULT_VERSION` 또는 `TELEMETRY_VERSION` 증가가 필요할 수 있습니다.

### R2 스토리지 및 클린업 규칙

- 대용량 텔레메트리 가공 원본 데이터는 Supabase DB에 적재하지 않고, Cloudflare R2 버킷 `bgms`에 저장합니다.
- `scripts/cleanup_telemetry.ts` 스크립트를 사용해 고립 파일을 지울 때는 R2 API의 벌크 삭제(`deleteMultipleFromR2`)를 사용합니다.
- R2 서비스 임포트 시 Next.js 절대 경로(`@/`)를 사용할 수 없는 단독 실행 스크립트에서는 상대 경로(`../lib/pubg-analysis/r2Service`)를 유지합니다.

### 텔레메트리 필드

PUBG 텔레메트리 필드는 `.pubg-telemetry-guide.md`를 우선 참고합니다.

- `maker`, `dBNOMaker`: 기절/DBNO 원인 추적
- `killer`, `finisher`: 최종 사살/마무리 추적
- `reviver`: 부활/소생 추적
- `attacker`, `victim`: 피해/교전 이벤트 추적
- `dBNOId`: 기절과 확킬/트레이드 인과관계 연결

필드명을 단순 추측으로 바꾸지 않습니다. `LogPlayerKillV2`, `LogPlayerMakeGroggy`, `LogPlayerTakeDamage`, `LogPlayerRevive`는 서로 의미가 다릅니다.

### 지도/좌표 수정

지도 좌표나 타일 처리는 `lib/map_config.ts`, `components/map/MapView.tsx`, `components/map/telemetry/*`를 함께 봅니다.

- 맵 크기: 8192, 4096, 3072, 2048 계열
- 원본 텔레메트리 좌표의 cm 단위 변환
- 맵별 y축 보정
- 리플레이 좌표와 일반 마커 좌표의 기준 일치

### Supabase/보안 수정

관리자 기능은 클라이언트 화면의 가드만으로 끝내지 않습니다. `app/api/admin/*` 라우트에서 반드시 서버 측 세션과 `profiles.role === "admin"` 검증을 수행합니다.

Service Role Key는 서버 라우트에서만 사용합니다. 클라이언트 컴포넌트에 노출하지 않습니다.

### RSC/Client 분리

페이지는 가능한 Server Component로 유지하고, 브라우저 API, Leaflet, localStorage, 상태/이벤트가 필요한 부분만 Client Component로 분리합니다. 지도와 통계 UI는 클라이언트 로직이 많으므로 import 경계를 특히 조심합니다.

## 13. 기능별 수정 체크리스트

기능을 고칠 때는 화면, API, 도메인 로직, 캐시, 테스트를 한 묶음으로 봅니다.

### 전적 검색/플레이어 캐시

- 화면: `app/stats/page.tsx`, `app/stats/[platform]/[nickname]/page.tsx`, `components/StatSearch.tsx`
- API: `app/api/pubg/player/route.ts`, `app/api/pubg/suggest/route.ts`
- 데이터: `pubg_player_cache`, `pubg_api_status`, `pubg_api_errors`
- 주의: 기본 전적 검색에 무기 숙련도 API를 다시 붙이지 않습니다.
- 테스트: `tests/suggest-players.test.ts`, 관련 route 타입 검사

### 매치 분석/전술 점수

- 화면: `components/stat/*`, `app/stats/[platform]/[nickname]/page.tsx`
- API: `app/api/pubg/match/route.ts`, `app/api/pubg/ingest/route.ts`
- 로직: `lib/pubg-analysis/AnalysisEngine.ts`, `lib/pubg-analysis/handlers/*`, `lib/pubg-analysis/benchmarkScore.ts`
- 데이터: `processed_match_telemetry`, `match_master_telemetry`, `match_stats_raw`, `global_benchmarks`
- 주의: 분석 결과 구조가 바뀌면 `RESULT_VERSION`과 클라이언트 파싱을 같이 확인합니다.
- 테스트: `tests/analysis-engine.test.ts`, `tests/squad-analysis.test.ts`

### 텔레메트리/지도 리플레이

- 화면: `components/map/telemetry/*`, `components/replay/*`, `app/replay/3d/page.tsx`
- API: `app/api/pubg/telemetry/route.ts`
- 로직: `hooks/useTelemetry.ts`, `lib/pubg-analysis/handlers/MapReplayHandler.ts`
- 캐시: R2 `{matchId}_{nickname}_v{TELEMETRY_VERSION}_map.json`
- 주의: 대량 데이터는 DB보다 R2 캐시를 우선하고, 구조 변경 시 `TELEMETRY_VERSION`을 검토합니다.
- 테스트: 텔레메트리 fixture 기반 테스트와 모바일/데스크톱 수동 렌더링 확인

### 지도/마커/핫드랍

- 화면: `app/maps/[mapId]/page.tsx`, `components/map/MapShell.tsx`, `components/map/MapView.tsx`
- API: `app/api/pubg/hotdrop/route.ts`, `app/api/report/*`, `app/api/admin/approve/route.ts`, `app/api/admin/reject/route.ts`
- 데이터: `map_settings`, `categories`, `map_markers`, `pending_markers`, `hotdrop_heatmap`
- 주의: 좌표계는 PUBG cm 좌표, 이미지 픽셀 좌표, Leaflet 좌표가 섞입니다.
- 테스트: 지도 표시, 제보 승인/거절, 핫드랍 레이어 수동 확인

### 무기 마스터리/무기 도감

- 화면: `app/stats/[platform]/[nickname]/weapons/*`, `app/weapons/*`
- API: `app/api/pubg/player/weapon-mastery/route.ts`, `app/api/images/weapons/[key]/route.ts`
- 로직: `lib/pubg/weaponMastery.ts`, `lib/pubg-analysis/constants.ts`
- 데이터: `pubg_player_cache.weapon_mastery_data`, `mastery_updated_at`, `weapons`, `attachments`
- 주의: 무기 마스터리는 전용 페이지에서만 1회 호출하고 3시간 쿨다운을 지킵니다.
- 테스트: 카테고리 분류, 캐시 hit/source, 409 "전적 검색 먼저 필요" 시나리오

### 가방/상자/도감 데이터

- 화면: `app/backpack/*`, `app/crates/*`, `app/weapons/*`
- API/Action: `app/actions/crates.ts`, `app/api/admin/crates/*`, `app/api/images/*`
- 로직: `lib/backpackUtils.ts`, `lib/crateUtils.ts`, `lib/epicAboveItems.ts`
- 데이터: `weapons`, `attachments`, `ammo`, `consumables`, `throwables`, `vehicles`, `crate_templates`, `crate_items`
- 주의: 확률/무게/아이템 기준 데이터는 Supabase와 UI 계산 로직을 같이 검증합니다.
- 테스트: `__tests__/backpackUtils.test.ts`, `tests/crate-probability.test.ts`

### 커뮤니티/콘텐츠

- 화면: `app/board/*`, `components/board/*`, `components/CommentSection.tsx`
- API: `app/api/posts/write/route.ts`, `app/api/admin/posts/delete/route.ts`, `app/api/admin/patch-notes/sync/route.ts`
- 데이터: `posts`, `comments`, `post_likes`, `notifications`, `sync_history`
- 주의: HTML/이미지 입력은 XSS와 storage cleanup을 함께 확인합니다.
- 테스트: 게시글 작성/삭제 권한, 관리자 삭제, 패치노트 sync 수동 확인

### Admin Agent/운영 자동화

- 화면: `app/admin/bot/page.tsx`
- API: `app/api/admin/bot/run/route.ts`, `app/api/admin/agent/*`
- 로직: `lib/admin-agent/tools.ts`, `context-pack.ts`, `impact.ts`, `approval-execution.ts`, `monitor-trend.ts`
- 데이터: `agent_runs`, `agent_steps`, `agent_approvals`, `agent_memories`, `ai_usage_logs`
- 주의: 위험 작업은 승인 요청만 생성하고 자동 실행하지 않습니다.
- 테스트: `tests/admin-bot.test.ts`, `tests/admin-agent-api.test.ts`, `tests/security.test.ts`

## 14. Admin Agent 내부 역할 지도

`lib/admin-agent/*`는 파일 수가 많으므로 역할을 나눠서 봅니다.

- 도구 선언/실행: `tools.ts`, `tool-catalog.ts`
- 실행 기록/권한 검증: `logging.ts`, `redaction.ts`
- 승인 큐: `approvals.ts`, `approval-execution.ts`, `impact.ts`, `approval-advisor.ts`
- 운영판 데이터: `context-pack.ts`, `command-center-export.ts`, `mission-control.ts`, `action-board.ts`
- 점검/배포 준비: `monitor-trend.ts`, `self-test.ts`, `rollout.ts`, `final-readiness.ts`, `safety-audit.ts`
- 운영 리포트: `briefing.ts`, `daily-checkout.ts`, `owner-brief.ts`, `handoff.ts`, `incident-timeline.ts`
- 기억/학습: `memory-suggestions.ts`, `agent_memories` 관련 API
- 성장/개선: `growth-roadmap.ts`, `improvement-backlog.ts`, `operator-value.ts`, `outcome-review.ts`
- 대응 지식: `playbooks.ts`, `risk-radar.ts`, `next-actions.ts`, `operating-sop.ts`

Agent 기능을 추가할 때는 `tools.ts`에 도구를 추가한 뒤 안전 등급, approval 필요 여부, UI 표시, 테스트를 함께 맞춥니다.

## 15. 테스트와 점검 루틴

변경 범위가 작아도 타입 검사와 관련 테스트는 먼저 돌립니다.

```bash
npx tsc --noEmit
npx eslint
npx vitest run tests/admin-agent-api.test.ts tests/admin-bot.test.ts tests/security.test.ts
npx vitest run tests/analysis-engine.test.ts tests/squad-analysis.test.ts
npm test -- --runInBand
```

수동 점검 대표 시나리오는 아래를 우선합니다.

- `/admin/bot`: 운영판, 승인 패널, 수동 점검, 기억/실행 기록 확인
- `/stats`: 플레이어 검색, 재검색, 매치 분석, AI 요약 확인
- `/stats/{platform}/{nickname}/weapons`: 무기 숙련도 갱신, 카테고리 탭, 3시간 캐시 확인
- `/maps/erangel`: 지도 타일, 마커, 핫드랍, 제보, 리플레이 진입 확인
- `/backpack`, `/crates`, `/weapons`: 기준 데이터 로드와 계산/확률 표시 확인

## 16. 유지보수 기본 원칙

처음 보는 개발자나 AI는 아래 순서로 접근합니다.

1. 사용자가 보는 화면 경로를 먼저 찾습니다.
2. 화면에서 호출하는 API route를 찾습니다.
3. API route가 읽고 쓰는 Supabase 테이블과 R2 키를 확인합니다.
4. 분석/도메인 로직은 `lib/*`에서 찾습니다.
5. 캐시가 있으면 메모리 캐시, DB 캐시, R2 캐시, ISR/태그 캐시를 구분합니다.
6. 관리자 기능이면 서버 측 admin 검증과 Service Role Key 노출 여부를 먼저 점검합니다.
7. PUBG API를 추가 호출하는 변경은 rate limit 영향부터 계산합니다.
8. 위험 작업은 직접 실행 로직보다 승인 큐 흐름에 태우는 것을 기본값으로 둡니다.

## 17. 코드 품질 규칙

- 함수나 타입을 신설/수정할 때 호출부와 선언부의 타입이 일치하는지 확인합니다.
- 사용하지 않는 import, 주석 처리된 코드, 의미 없는 `console.log`, 하드코딩된 더미 데이터는 남기지 않습니다.
- 대용량 텔레메트리 데이터는 DB 행에 직접 넣기보다 Cloudflare R2 캐시를 우선합니다.
- 프롬프트/AI 응답 JSON 구조를 바꾸면 클라이언트 파싱과 에러 fallback을 함께 수정합니다.
- 성능에 민감한 지도/리플레이 변경은 데스크톱과 모바일 레이아웃을 모두 확인합니다.
- 위험한 운영 액션은 자동 실행하지 말고 Admin Agent 승인 흐름을 사용합니다.

## 18. 자주 쓰는 탐색 명령

```bash
rg "r2Service|uploadToR2|downloadFromR2"
rg "CLOUDFLARE_R2_"
rg "AnalysisEngine"
rg "RESULT_VERSION|TELEMETRY_VERSION"
rg "playback"
rg "processed_match_telemetry|match_master_telemetry"
rg "profile.*role|role.*admin"
rg "admin-agent|agent_runs|agent_approvals"
rg "weapon-mastery|weapon_mastery_data|WeaponMastery"
rg "PUBG_QUOTA|pubg_api_status|trackPubgRateLimit"
rg "ADMIN_AGENT_CRON_SECRET|CRON_SECRET"
rg "Record Agent Monitor Snapshot|daily-tasks"
rg "app/api/pubg|app/api/admin/agent"
rg "pubg_player_cache|weapon_mastery_data|mastery_updated_at"
rg "processed_match_telemetry|TELEMETRY_VERSION|RESULT_VERSION"
rg "agent_runs|agent_steps|agent_approvals|agent_memories"
rg "app/weapons|app/backpack|app/crates|app/rankings"
```

이 문서와 실제 코드가 충돌하면 실제 코드가 우선입니다. 문서가 틀린 것을 발견하면 변경 작업과 함께 이 파일도 갱신합니다.
