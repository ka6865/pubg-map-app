// app/stats/battle/layout.tsx
// 배틀 페이지 레이아웃 — 메타데이터는 page.tsx의 generateMetadata에서 처리합니다.

export default function BattleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
